Original prompt: [@sites] 괴혼 스타일의 3D 웹게임을 만들어줘. 제조, 건설, 수송, 통신, 생명을 주제로 시대별 아이템을 모으고 미래로 갈수록 거대한 아이템을 모으는 것이 목표이며, 초등학생도 쉽게 할 수 있는 인터페이스로 최대한 원작에 가까운 재미를 원함.

## 진행 기록

- Sites 스타터를 `work/site`에 초기화하고 개발 서버를 시작함.
- 저작권이 있는 원작의 캐릭터·UI·음악·레벨·고유 명칭은 복제하지 않고, 구체를 굴려 작은 물건부터 모으며 커지는 핵심 장르 문법만 사용하는 독창적 게임으로 방향을 확정함.
- 게임명은 `데굴데굴 시간공작소`, 플레이어 캐릭터는 시간정비 로봇 `토리`, 수집 구체는 `시간 구슬`로 설정함.
- 외부 3D 라이브러리 없이 브라우저 기본 WebGL로 실제 원근·조명·입체 메시를 렌더링하도록 구현함.
- 키보드 이동/부스터/브레이크/일시정지/전체화면, 모바일 조이스틱/부스터, 5개 시대, 주제별 수집, 크기 성장, 제한 시간, 시대 완료/재도전/최종 승리를 구현함.
- `window.render_game_to_text`, `window.advanceTime`, 로컬 진행 저장을 연결함.
- 첫 프로덕션 빌드가 성공함.
- Playwright 실제 조작: 시작 후 전진 300프레임으로 제조 아이템 8개를 모아 `eraClear`에 도달했고, 콘솔 오류가 없었음. 3D 캔버스 스크린샷도 직접 확인함.
- 코드 리뷰에서 발견된 큐브 법선 데이터, WebGL 오류 화면 우선순위, 조이스틱 첫 터치, 모바일 오버레이 스크롤, 핀치 줌, 린트/타입 검사 문제를 모두 수정함.
- 실제 키보드 조작으로 5개 시대의 목표 아이템을 차례로 모으고 최종 미래 생태돔까지 수집하여 `victory`에 도달함.
- 데스크톱/모바일 시작 화면, 플레이 화면, 시대 완료, 시간 초과, 최종 승리를 확인했고 모바일 조이스틱은 누르는 즉시 이동함.
- 전용 소셜 공유 이미지 `public/og.png`를 생성·검수하고 요청 호스트 기준의 절대 Open Graph/X 이미지 주소를 연결함.
- 최종 검증: 빌드, ESLint, TypeScript, 서버 렌더 테스트 2/2, 브라우저 E2E 6/6, 콘솔 오류 0, 프로덕션 의존성 보안 감사 0건.
- Sites 프로젝트 `데굴데굴 시간공작소`를 생성하고 프로젝트 ID를 호스팅 설정에 저장함.

## 남은 일

- 최종 소스 커밋·패키징·Sites 비공개 배포와 배포 주소 확인

## 2026-07-30 그래픽·카메라 개선 요청

추가 요청: 그래픽 품질이 낮고 시점이 고정되어 다이내믹한 느낌이 부족함.

- 현재 화면을 기준 이미지로 보존하고, 속도·방향 전환·성장에 반응하는 추적 카메라로 교체한다.
- 카메라 회전, 전방 시야 확보, 속도별 FOV, 회전 뱅킹, 수집 줌 펄스를 추가한다.
- 조명·림라이트·거리 안개·시대별 배경 구조물·구름·속도 잔상을 추가해 평면적인 격자 무대를 입체적인 장면으로 개선한다.
- 키보드와 터치 이동, 5개 시대 완주, 저감 모션 환경은 기존 동작을 유지한다.
- 고정 +Z 시점을 제거하고 이동 속도·방향·회전에 따라 추적 방향, 거리, 높이, FOV, 뱅킹이 변하는 카메라를 구현했다.
- 수집 시 카메라가 살짝 물러나는 성장 펄스와 방향성 충격 흔들림을 넣었으며, 모션 저감 환경에서는 흔들림과 뱅킹을 끈다.
- 셰이더에 키/필/림 조명, 반사 하이라이트, 거리 안개를 추가하고 구·원기둥 메시 해상도를 높였다.
- 시대별 외곽 랜드마크, 구름, 태양, 깊이 있는 지면, 속도 잔상, 상세 수집품과 로봇 방향 애니메이션을 추가했다.
- 화면 반복 검사 중 지면 원형 구조물이 검은 호처럼 보이던 문제를 발견해 제거했다.
- 카메라가 바라보는 방향을 기준으로 키보드·조이스틱 입력을 변환해, 시점이 회전해도 화면의 위쪽으로 자연스럽게 전진하도록 했다.
- 투명 물체를 카메라에서 먼 순서대로 모아 그리도록 바꾸고, 비균일 확대 물체의 법선 변환을 보정해 겹침과 조명 왜곡을 줄였다.
- 모바일·저성능 기기에는 메시 수, 장식물 수, 해상도 배율을 낮춘 적응형 품질을 적용하고 데스크톱은 고품질 메시를 유지했다.
- 모션 저감 환경에서는 흔들림·뱅킹·장식 애니메이션뿐 아니라 속도에 따른 줌·높이·전방 구도 변화도 정지한다.
- 시각 판정은 첫 캔버스 단독 화면 78점에서 HUD를 포함한 전체 플레이 화면 91점으로 개선되어 통과했다.
- 기존 브라우저 E2E 6/6을 다시 통과했고 키보드·모바일·5개 시대 최종 승리 및 콘솔 오류 0건을 확인했다.
- 최종 검증에서 빌드·서버 렌더 테스트 2/2, ESLint, TypeScript, `git diff --check`, 전용 WebGL 플레이 클라이언트를 모두 다시 통과했다.
