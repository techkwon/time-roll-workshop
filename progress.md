Original prompt: [@sites] 괴혼 스타일의 3D 웹게임을 만들어줘. 제조, 건설, 수송, 통신, 생명을 주제로 시대별 아이템을 모으고 미래로 갈수록 거대한 아이템을 모으는 것이 목표이며, 초등학생도 쉽게 할 수 있는 인터페이스로 최대한 원작에 가까운 재미를 원함.

## 진행 기록

- Sites 스타터를 `work/site`에 초기화하고 개발 서버를 시작함.
- 저작권이 있는 원작의 캐릭터·UI·음악·레벨·고유 명칭은 복제하지 않고, 구체를 굴려 작은 물건부터 모으며 커지는 핵심 장르 문법만 사용하는 독창적 게임으로 방향을 확정함.
- 게임명은 `데굴데굴 시간공작소`, 플레이어 캐릭터는 시간정비 로봇 `토리`, 수집 구체는 `시간 구슬`로 설정함.
- 외부 3D 라이브러리 없이 브라우저 기본 WebGL로 실제 원근·조명·입체 메시를 렌더링하도록 구현함.
- 키보드 이동/부스터/브레이크/일시정지/전체화면, 모바일 조이스틱/부스터, 5개 시대, 주제별 수집, 크기 성장, 제한 시간, 시대 완료/재도전/최종 승리를 구현함.
- `window.render_game_to_text`, `window.advanceTime`, 로컬 진행 저장을 연결함.
- 첫 프로덕션 빌드가 성공함.
- Playwright 실제 조작: 시작 후 전진 300프레임으로 제조 아이템 8개를 모아 `eraClear`에 도달했고, 콘솔 오류가 없었음. 3D 캔버스 스크린샷도 직접 확인함.
- 코드 리뷰에서 발견된 큐브 법선 데이터, WebGL 오류 화면 우선순위, 조이스틱 첫 터치, 모바일 오버레이 스크롤, 핀치 줌, 린트/타입 검사 문제를 모두 수정함.
- 실제 키보드 조작으로 5개 시대의 목표 아이템을 차례로 모으고 최종 미래 생태돔까지 수집하여 `victory`에 도달함.
- 데스크톱/모바일 시작 화면, 플레이 화면, 시대 완료, 시간 초과, 최종 승리를 확인했고 모바일 조이스틱은 누르는 즉시 이동함.
- 전용 소셜 공유 이미지 `public/og.png`를 생성·검수하고 요청 호스트 기준의 절대 Open Graph/X 이미지 주소를 연결함.
- 최종 검증: 빌드, ESLint, TypeScript, 서버 렌더 테스트 2/2, 브라우저 E2E 6/6, 콘솔 오류 0, 프로덕션 의존성 보안 감사 0건.
- Sites 프로젝트 `데굴데굴 시간공작소`를 생성하고 프로젝트 ID를 호스팅 설정에 저장함.

## 남은 일

- 최종 소스 커밋·패키징·Sites 비공개 배포와 배포 주소 확인

## 2026-07-30 그래픽·카메라 개선 요청

추가 요청: 그래픽 품질이 낮고 시점이 고정되어 다이내믹한 느낌이 부족함.

- 현재 화면을 기준 이미지로 보존하고, 속도·방향 전환·성장에 반응하는 추적 카메라로 교체한다.
- 카메라 회전, 전방 시야 확보, 속도별 FOV, 회전 뱅킹, 수집 줌 펄스를 추가한다.
- 조명·림라이트·거리 안개·시대별 배경 구조물·구름·속도 잔상을 추가해 평면적인 격자 무대를 입체적인 장면으로 개선한다.
- 키보드와 터치 이동, 5개 시대 완주, 저감 모션 환경은 기존 동작을 유지한다.
- 고정 +Z 시점을 제거하고 이동 속도·방향·회전에 따라 추적 방향, 거리, 높이, FOV, 뱅킹이 변하는 카메라를 구현했다.
- 수집 시 카메라가 살짝 물러나는 성장 펄스와 방향성 충격 흔들림을 넣었으며, 모션 저감 환경에서는 흔들림과 뱅킹을 끈다.
- 셰이더에 키/필/림 조명, 반사 하이라이트, 거리 안개를 추가하고 구·원기둥 메시 해상도를 높였다.
- 시대별 외곽 랜드마크, 구름, 태양, 깊이 있는 지면, 속도 잔상, 상세 수집품과 로봇 방향 애니메이션을 추가했다.
- 화면 반복 검사 중 지면 원형 구조물이 검은 호처럼 보이던 문제를 발견해 제거했다.
- 카메라가 바라보는 방향을 기준으로 키보드·조이스틱 입력을 변환해, 시점이 회전해도 화면의 위쪽으로 자연스럽게 전진하도록 했다.
- 투명 물체를 카메라에서 먼 순서대로 모아 그리도록 바꾸고, 비균일 확대 물체의 법선 변환을 보정해 겹침과 조명 왜곡을 줄였다.
- 모바일·저성능 기기에는 메시 수, 장식물 수, 해상도 배율을 낮춘 적응형 품질을 적용하고 데스크톱은 고품질 메시를 유지했다.
- 모션 저감 환경에서는 흔들림·뱅킹·장식 애니메이션뿐 아니라 속도에 따른 줌·높이·전방 구도 변화도 정지한다.
- 시각 판정은 첫 캔버스 단독 화면 78점에서 HUD를 포함한 전체 플레이 화면 91점으로 개선되어 통과했다.
- 기존 브라우저 E2E 6/6을 다시 통과했고 키보드·모바일·5개 시대 최종 승리 및 콘솔 오류 0건을 확인했다.
- 최종 검증에서 빌드·서버 렌더 테스트 2/2, ESLint, TypeScript, `git diff --check`, 전용 WebGL 플레이 클라이언트를 모두 다시 통과했다.

## 2026-07-30 절대 품질 재검수 및 재질 스킨 요청

추가 피드백: 이전 검수가 상대평가에 머물렀고 현재 화면은 상용 게임이 아니라 초등학교 프로젝트 수준으로 보임. 그래픽 효과를 높이기 위해 코덱스 이미지 생성으로 만든 스킨을 3D 오브젝트에 적용하기로 함.

- 기존 플레이 화면을 상용 스타일라이즈드 3D 웹게임 기준으로 다시 평가했으며 절대 점수는 58/100으로 판정했다.
- 핵심 결함은 넓고 평평한 격자 지면, 약한 접지감과 재질 구분, 혼란스러운 주인공 실루엣, 무작위로 흩어진 수집품, 빈 배경, 웹앱처럼 무거운 HUD였다.
- 목표를 ‘초등학생도 쉬운 조작 + 프리미엄 장난감 디오라마 그래픽’으로 재설정했다.
- 코덱스 이미지 생성으로 3×3 재질 아틀라스를 제작했다: 시간 구슬 오렌지 에나멜, 목재·황동, 테라코타, 건설용 도장 강철, 수송용 파란 금속·고무, 통신 회로, 잎맥 바이오플라스틱, 생태돔 유리, 아이보리 세라믹.
- 원본 생성 이미지는 보존하고 웹용 JPEG 아틀라스를 `public/textures/time-roll-material-atlas.jpg`에 배치했다.
- WebGL 메시의 UV 좌표와 3×3 아틀라스 샘플링 기능을 추가해 각 오브젝트가 주제별 스킨을 사용할 수 있는 기반을 만들었다.
- 제조·건설·수송·통신·생명 수집품을 주제별 3가지 실루엣으로 나누고, 초점 주제·가치 단계에 따라 크기·받침 링·금빛 글린트를 구분했다.
- 시간정비 로봇 토리는 두 팔로 시간 구슬의 크로노 핸들을 미는 명확한 포즈로 바꾸고, 생성 스킨의 배율·혼합량을 물체 크기에 맞게 조정해 표면 노이즈를 줄였다.
- 시대별 대형 랜드마크, 분기 길, 작업장, 길 안내 배너·조명 기둥, 테마 활동 구역을 추가해 빈 지면을 줄이고 수집 동선을 전경·중경·배경으로 연결했다.
- 카메라는 이동 방향 회전, 속도 FOV, 성장 펄스뿐 아니라 회전 시 최대 약 11도의 뱅킹을 보이며, 전용 검증에서 고정 시점 회귀를 차단했다.
- 모바일 HUD를 2행으로 압축하고 상단 동작 버튼을 가로 배치했으며, 세로 화면에서 시간 구슬 크기를 24~27% 수준으로 낮추고 로봇을 완전히 보이게 했다.
- 최종 절대 품질 판정은 데스크톱 정지·회전, 모바일 세로, 5개 시대 접촉면을 함께 보고 90/100 `PASS`를 받았다.
- 전용 WebGL 시각 게이트는 데스크톱 정지·속도·회전, 5개 시대, 모바일 세로/가로, 모션 저감에서 실패 0건과 브라우저 오류 0건을 기록했다.
- 브라우저 E2E는 실제 키보드 수집, 일시정지/재개, 재도전, 5개 시대와 최종 생태돔 승리, 모바일 조이스틱까지 6/6 통과했다.

## 2026-07-30 실서버 직접 재검수 및 오브젝트 다양화

추가 피드백: 실서버에서 바닥이 번져 보이고 캐릭터가 깨져 보이며, 생성 이미지를 적용한 오브젝트의 수와 종류가 부족함.

- 실서버 게임 화면에 직접 진입해 바닥의 넓은 얼룩형 패치, 로봇 부품 분리감, 중앙 동선의 반복 오브젝트를 재현했다.
- 원인은 비타일 재질 이미지를 대형 지면에서 반복한 구조, 서로 겹치는 평면의 깊이 충돌, 로봇 부품의 혼합 좌표계, `id % 3`에 묶인 실루엣 종류로 확인했다.
- 코덱스 이미지 생성으로 제조·건설·수송·통신·생명 5개 주제의 5×5 오브젝트 아틀라스를 새로 제작했다.
- 마젠타 크로마키를 사용하는 별도 WebGL 오브젝트 텍스처와 양면 데칼 평면을 연결해 생성 이미지를 실제 수집품에 표시했다.
- 주제별 10종, 전체 50종의 실루엣과 이미지 조합을 구현하고 한 시대당 수집품을 99개 수준으로 늘렸다.
- 초반 좌우 쇼케이스와 동선 주변·외곽 배치를 나누고 중앙에 몰린 일부 수집품을 좌우 활동 구역으로 분산했다.
- 대형 지면에서 생성 재질 반복을 제거하고, 서로 높이가 분리된 불투명 테두리형 길 타일과 각형 활동 플랫폼으로 바꿨다.
- 지면 상·하판이 같은 높이에서 겹쳐 생기던 수평 줄무늬를 제거했다.
- 로봇의 몸·머리·눈·다리·팔·손을 하나의 로컬 좌표계로 다시 조립하고, 양손과 시간 구슬 사이에 명확한 손잡이와 소켓을 연결했다.
- 절대 시각 판정은 실제 플레이 캡처 기준 82점 → 86점 → 91점으로 개선되어 `PASS`를 받았다.
- 표준 웹게임 Playwright 클라이언트로 수정 후 플레이 화면을 생성하고 최신 캡처를 직접 확인했다.

## 현재 검증 상태

- 데스크톱 전체 게임 E2E 6개 시나리오를 다시 실행해 6/6 통과했다.
- 모바일 세로 화면에서 전체 캐릭터·시간 구슬·터치 조작·HUD가 함께 보이는 것을 직접 확인했다.
- 제조·건설·수송·통신·생명 5개 시대의 플레이 캡처를 직접 비교해 생성 이미지 오브젝트가 시대별로 바뀌는 것을 확인했다.
- 깨끗한 새 브라우저 세션에서 실제 플레이를 다시 시작하고 콘솔 오류·경고 0건을 확인했다.
- 최종 빌드 후 Sites 새 버전을 배포하고 실서버를 다시 검수한다.

## 2026-07-30 판매 가능한 수준의 전면 업그레이드

새 목표: 실제 판매는 하지 않더라도 유료 출시 심사를 받을 수 있는 독립 웹게임 수준으로 완성한다.

추가 핵심 요구:

- 시간 구슬은 처음에 로봇보다 훨씬 작게 시작한다.
- 현재 구슬보다 큰 물건은 충돌해도 붙지 않고, 작은 물건을 모아 성장한 뒤에만 붙는다.
- 각 시대는 작은 물건 → 중간 물건 → 큰 물건 → 거대 시대 목표의 단계적 성장으로 진행한다.
- 랜딩은 흰 안내 카드가 아니라 실제 게임 장면과 캐릭터·성장 약속이 먼저 보이는 게임 타이틀 화면으로 개편한다.

현재 상용화 감사 결과:

- 1시대 시작 크기가 45cm이고 중앙 목표 아이템이 모두 즉시 수집 가능해 성장 잠금이 사실상 작동하지 않는다.
- 시대 목표 개수만 채우면 거대 특수 아이템을 모으기 전에 종료되어 크기 성장의 보상감이 약하다.
- 시대 전환마다 구슬과 부착물이 초기화되고, 저장은 최고 시대·크기만 남아 재도전 동기가 얕다.
- 인트로의 흰 설명 패널과 세로 시대 목록이 3D 장면을 가려 교육용 안내 화면처럼 보인다.
- 99개 수집품 전체를 거리 제한 없이 그려 모바일 draw-call 예산을 지키기 어렵다.

당시 계획:

- 시작 반경·수집 임계값·성장 티어·거대 목표 잠금을 순수 규칙 모듈과 회귀 테스트로 분리한다.
- 전용 16:9 키아트를 생성해 `public/time-roll-key-art.jpg`로 준비했다.
- 새 랜딩은 full-bleed 키아트, 좌측 게임 로고·성장 약속·CTA, 하단 시대 레일로 구성한다.

## 2026-07-30 로봇 토리 스토리·성장·출시 전 검증 완료

- 주인공을 시간수리 로봇 `토리(TR-05)`로 확정하고, 멈춰 버린 미래 도시의 시간동력핵을 되살리기 위해 제조·건설·수송·통신·생명의 다섯 기술 코어를 회수하는 세계관을 추가했다.
- 각 시대에 임무 브리핑, 거대 목표 이름, 클리어 문구를 연결하고 마지막 미래 생태돔 수집 후 `시간 전시관 완성` 엔딩이 표시되도록 했다.
- 모든 시대의 구슬은 기준 반경의 18%에서 시작하고, `tiny → small → medium → large → monument` 티어를 거쳐 성장한다.
- 구슬보다 큰 물건은 충돌 안내만 표시하고 붙지 않으며, 수집 가능 크기에 도달한 뒤 같은 물건을 다시 만나면 정상적으로 붙는다.
- 주제 물건 목표 개수를 채워도 곧바로 시대가 끝나지 않고, 해금된 거대 목표를 실제로 수집해야만 시대 완료 또는 최종 승리로 전환된다.
- 미션 수치는 목표를 초과해 수집해도 `8/8`까지만 표시하도록 보정했다.
- 시대 점수·최고 콤보·S/A/B 랭크·완료 여부·누적 최고 점수·엔딩 확인·사운드 설정을 v2 저장 형식으로 보존하고, 기존 `{bestEra,bestSize}` 저장값을 자동 마이그레이션한다.
- 인트로를 로봇 토리 키아트가 전면에 보이는 full-bleed 게임 타이틀 화면으로 교체하고, 8cm 시작부터 시대 상징물까지의 성장 약속과 다섯 시대 레일을 배치했다.
- WebGL 렌더러에 거리·우선순위 컬링과 저비용 원거리 LOD를 추가해 모바일 검증에서 `drawCalls 108`, `transparentCalls 31`을 기록했다.
- 최종 시각 판정은 이전/현재 랜딩·플레이, 모바일, 시대 완료, 최종 승리 화면을 함께 비교해 92/100 `PASS`를 받았다.
- 최신 정확한 소스 상태에서 `npm test` 15/15, 브라우저 E2E 12/12, TypeScript, ESLint, `git diff --check`, 브라우저 오류 0건을 통과했다.

## 남은 배포 절차

- 검증된 정확한 소스를 Lore 형식으로 커밋하고 원격 저장소에 푸시한다.
- 해당 커밋으로 Sites 버전을 저장·비공개 배포한 뒤 실제 배포 주소의 데스크톱·모바일·콘솔 상태를 다시 확인한다.

## 2026-07-30 회전 조립 결함 및 생성 이미지 대확장

추가 피드백: 방향을 바꾸면 로봇의 청록색 얼굴·가슴 패널과 팔이 판처럼 튀어나오고, 건물의 기둥·지붕·부속물이 서로 다른 좌표계로 보이며, 생성 이미지의 실제 사용량이 부족함.

- 사용자 캡처와 동일하게 회전 장면을 재현해 로봇 부품 배치 기준과 렌더러 yaw 부호가 반대인 것을 수치로 확인했다.
- 로봇 메시 회전을 `-heading`으로 통일하고, 팔을 수평 길이·세로 높이를 억지로 늘린 판에서 두 점을 정확히 연결하는 3차원 세그먼트로 교체했다.
- 로봇 몸·얼굴 패널의 돌출 깊이를 줄이고 0도·±90도·180도 네 방향 회귀 테스트와 순수 좌표 수학 테스트를 추가했다.
- 과도한 카메라 뱅킹을 최대 약 6도로 낮추고 입력 해제 후 빠르게 수평으로 복귀하도록 감쇠를 조정했다.
- 작업장·물레방아·공사 구조물·수송 차량·통신탑·생태돔의 문·창·허브·콘솔·현관을 모두 같은 로컬 회전 기준으로 배치했다.
- 시대 1 대표 건물의 플랫폼과 본체가 서로 다른 yaw를 쓰던 결함을 제거하고 주변 상자도 플랫폼 로컬 좌표로 배치했다.
- 나무 보조 수관, 건설 표지, 활동 섬 소품, 여러 수집품의 자식 부품에 남아 있던 월드 좌표 오프셋을 로컬 좌표로 변환했다.
- 코덱스 이미지 생성으로 5×5 고품질 재질 25종, 신규 수집물 25종, 환경 수집물 25종, 건물 외장 모듈 25종을 추가했다.
- 기존 수집물 25종까지 포함해 총 125개 생성 이미지 셀을 준비하고, 2,000px 이하 텍스처로 나눠 WebGL 저사양 제한을 지켰다.
- 수집물 변형을 주제별 15종·전체 75종으로 늘리고, 생성된 환경 물체까지 실제 아이템 시퀀스에서 선택되도록 아틀라스 인덱스를 연결했다.
- 5×5 재질 아틀라스를 지면 길·벽·지붕·굴뚝·차량·타이어·서버·나무·생태돔에 적극 적용했다.
- 25개 외장 이미지는 카메라를 따라다니는 장식판이 아니라 실제 건물의 평평한 앞면에 작은 깊이 여유를 두고 밀착 배치했다.
- 현재 로컬 E2E는 14/14, 브라우저 오류 0건이며 네 방향 로봇과 다섯 시대 contact sheet를 생성했다.
- 남은 절차는 다섯 시대 대표 건물 근접 캡처, 전체 빌드·테스트, 동일 커밋의 Sites 배포 및 실서버 재검수다.

## 2026-07-30 이미지 소스 150종 확충 및 최종 시각 게이트

- 제조·건설·수송·통신·생명 주제의 신규 경관·설비 5×5 아틀라스 25종을 추가 생성했다.
- 신규 환경 이미지를 기존 환경 아틀라스와 결합해 10×5, 50종으로 확장하고, 주제별 오브젝트 변형을 20종·전체 100종까지 늘렸다.
- 재질 25종, 수집·환경 오브젝트 100종, 건물 외장 25종으로 실제 게임이 참조하는 생성 이미지 소스는 총 150종이다.
- 다섯 시대마다 3D 받침·지지대가 있는 이미지 전시물과 길가 키오스크를 배치하고, 경로 레일·지형 구역·랜드마크에도 생성 텍스처를 적용했다.
- 모바일에서 갈색 직사각형으로 보이던 수집 파티클을 투명 구체로 교체하고, 원거리 데칼은 저비용 3D 실루엣으로 바꿨다.
- 로봇은 0도·±90도·180도 네 방향에서 몸통·얼굴·팔이 깨지지 않으며, 다섯 시대 건물은 지붕·기둥·부속물이 같은 로컬 좌표계로 결합된 것을 근접 캡처로 확인했다.
- 최종 브라우저 E2E는 16/16, 모바일 렌더 예산은 draw call 135·transparent call 22, 근접 시대 캡처의 마젠타 누출은 모두 0픽셀을 기록했다.
- 검수 스크립트는 재질·기본 오브젝트·환경 오브젝트·건물 외장 아틀라스 4개가 모두 로드된 뒤에만 화면 비교를 시작하도록 강화했다.
- 네 번째 절대 시각 판정은 91/100 `PASS`였고, 갈색 직사각형·로봇 회전 깨짐·마젠타 점·주요 구조물 분리 현상이 모두 없음을 확인했다.
